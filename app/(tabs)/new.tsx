// Placeholder route for the center "New" tab. The tab's listener intercepts
// the press and routes to /new/setup, so this screen is never actually shown.
import { View } from 'react-native';

export default function NewTabPlaceholder() {
  return <View />;
}
